# thinkppt

## 项目简介

一个桌面端 AI PPT 生成工具。用户输入一句话描述要做的一次汇报，产品按**理解 → 重构 → 呈现**三阶段推进：先看清这件事的结构，再用 5W2H / AB+SCQA+SAP / 金字塔 MECE 这套方法把结构重新组织清楚，最后在应用里逐层确认展开成一棵持久保存的树，随时可以接着理、随时一键成稿成 PPT（职场汇报、学术答辩）。

App 名称 / 仓库名：**thinkppt**（纯英文名，无中文品牌），定位：先想清楚、再一键成稿的 AI PPT 工具，面向职场汇报和学术答辩。副标题："AI 生成 PPT 不难，难的是生成一份你敢讲的。"命名的来龙去脉见 `docs/产品策划.md`"命名"一节。

核心理念："你只需要告诉我要做什么，我帮你想清楚它是什么样子，重新组织好，这份清楚随时留在这里，你可以接着想、也可以带出去用。"

**注意**：这不是一个任务计划表/待办工具——"打勾表示做完"这类执行追踪功能是评估后主动放弃的方向。"呈现"也不是生成一份文档就结束，而是贯穿全程的"应用内持久化 + 随时可续接 + 随时可导出"。曾经设想过用"锁定式渐进执行"（不确认上一步看不到下一步）当差异化机制，已评估放弃——它只能防用户跳着看结构，防不住用户不做实际工作，不是真正的差异化。目前站得住脚的是更朴素的一点：结构和进度持久保存、隔几天回来能低成本接续，这是跟"直接打开 ChatGPT 聊"的核心区别。详见 `docs/产品策划.md` 的差异化评估。

## 技术栈

- Electron + electron-vite（桌面框架 + 构建）
- React 19 + TypeScript（前端 UI）
- Tailwind CSS 4（样式）
- Framer Motion（动画）
- electron-store（本地设置）
- OpenAI SDK / Anthropic SDK（AI 多模型支持）

## 项目结构

- `electron/` — Electron 主进程（窗口管理、AI API 调用、IPC）
- `src/` — React 渲染进程（UI 组件、hooks、AI 类型定义）
- `src/components/` — UI 组件
- `src/lib/ai/` — AI provider 抽象层
- `docs/` — 产品策划和技术方案文档

## 开发命令

- `npm run dev` — 启动开发模式（hot reload）
- `npm run build` — 打包生产版本
- `npm run preview` — 预览生产构建

## 架构要点

1. AI API 调用在 Main 进程，API Key 不暴露给 Renderer
2. 通过 IPC + contextBridge 通信
3. AI Provider 是可插拔的：OpenAI / Claude / DeepSeek
4. 数据模型（现状）：Plan → PlanStage[] → SubStep[]，实现的是"drafting → expanding"两阶段锁定式状态机（旧版本，将保留复用为"呈现"阶段的执行引擎）
5. 数据模型（新方向，待实现）：需要能区分"理解阶段产出的结构"和"重构/呈现阶段的执行计划"两层数据，并新增持久化与导出模块，详见 `docs/技术方案.md`

## 当前状态

MVP 已跑通旧版两阶段流程（拟定大纲 → 锁定式逐步展开）。产品方向已确定演进为**理解 → 重构 → 呈现**三阶段框架，代码尚未按新方向重构，下一轮实现前需先修复几个已知代码问题（类型缺字段、已确认阶段上下文链路断裂、无持久化，详见 `docs/技术方案.md`"现状代码基线与已知问题"一节）。详细技术方案见 `docs/技术方案.md`，产品策划见 `docs/产品策划.md`。
